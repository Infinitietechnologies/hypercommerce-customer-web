// DEPRECATED (hyperlocal). The zone/location picker was removed in the
// hypercommerce migration — market is auto-detected server-side and a
// market/currency switcher will replace this in a later phase. Kept as a
// no-op stub so any lingering import resolves; no longer rendered anywhere.
const LocationSelector = () => null;

export default LocationSelector;
